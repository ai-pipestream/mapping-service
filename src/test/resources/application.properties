# Test Configuration for mapping-service
#
# Keep tests hermetic: do not attempt service registration or external discovery.
%test.pipestream.registration.enabled=false

# Test HTTP port (avoid conflicts)
quarkus.http.test-port=9667

# Logging for tests
quarkus.log.level=INFO
quarkus.log.category."ai.pipestream.service.mapping".level=DEBUG
